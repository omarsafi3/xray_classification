# =============================================================================
# X-ray Classification System - Environment Variables
# =============================================================================
# Copy this file to .env and customize values for your environment
# Usage: docker-compose --env-file .env up -d
# =============================================================================

# Database Configuration
DB_PASSWORD=your_secure_password_here

# JWT Configuration (generate a secure random key)
JWT_SECRET=your_secure_jwt_secret_key_at_least_32_characters

# Data Paths (host machine paths)
TEST_DATA_PATH=./data/test
CLIENT_DATA_PATH=./data/client

# Optional: Override default ports
# BACKEND_PORT=8080
# FRONTEND_PORT=4200
# FL_SERVER_PORT=8081
# MYSQL_PORT=3306
